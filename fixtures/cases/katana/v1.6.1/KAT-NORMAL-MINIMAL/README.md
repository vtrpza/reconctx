# KAT-NORMAL-MINIMAL

Loopback-only Katana v1.6.1 JSONL fixture. It contains six GET/200 crawl observations. The target exposed an external link, but the crawl scope regex prevented it from entering output. `stdout.raw` and the native output were byte-identical in the private capture.
