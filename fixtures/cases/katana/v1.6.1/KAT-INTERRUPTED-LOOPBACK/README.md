# KAT-INTERRUPTED-LOOPBACK

Katana v1.6.1 was interrupted by the bounded outer timeout after three valid loopback JSONL records. The fixture proves partial-output preservation only; it does not represent complete crawl coverage. The private manifest was not rewritten; this public copy derives the version from its immutable `version.txt`.
